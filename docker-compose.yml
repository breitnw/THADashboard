version: "1.0"
services:
  web:
    build: .
    ports:
      - 5000:5000
    depends_on:
      - redis
  redis:
    image: redis:latest
    volumes:
      - redis_data:/data
    ports:
      - 6379:6379
volumes:
  redis_data: {}
